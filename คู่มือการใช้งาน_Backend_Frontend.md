# 📖 คู่มือการใช้งาน YouTube Transcript API - Backend & Frontend

## 🎯 ภาพรวม

โปรเจกต์นี้ประกอบด้วย 2 ส่วนหลัก:
1. **Backend Service** (FastAPI) - API สำหรับดึง transcript และแปลงเป็นไฟล์
2. **Frontend** (React) - เว็บแอปพลิเคชันสำหรับใช้งาน

---

## 📋 ขั้นตอนการติดตั้งและใช้งาน (Step by Step)

### ส่วนที่ 1: ติดตั้ง Backend

#### ขั้นตอนที่ 1.1: ตรวจสอบ Python

```bash
python3 --version
```

**ต้องเป็น Python 3.8 หรือสูงกว่า** (แต่ต่ำกว่า 3.15)

#### ขั้นตอนที่ 1.2: สร้าง Virtual Environment

```bash
# เข้าไปในโฟลเดอร์ backend
cd backend

# สร้าง virtual environment
python3 -m venv venv

# เปิดใช้งาน
source venv/bin/activate  # Linux/Mac
# หรือ
venv\Scripts\activate     # Windows
```

**สังเกต:** เมื่อเปิดใช้งานแล้ว จะเห็น `(venv)` หน้าพรอมต์

#### ขั้นตอนที่ 1.3: ติดตั้ง Dependencies

```bash
pip install -r requirements.txt
```

**รอให้ติดตั้งเสร็จ** (อาจใช้เวลา 2-3 นาที)

#### ขั้นตอนที่ 1.4: รัน Backend Server

```bash
python main.py
```

**หรือ**

```bash
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

**ผลลัพธ์:**
- Backend จะรันที่: **http://localhost:8000**
- API Documentation: **http://localhost:8000/docs**

**✅ ถ้าเห็นข้อความ "Application startup complete" แสดงว่าสำเร็จ!**

---

### ส่วนที่ 2: ติดตั้ง Frontend

#### ขั้นตอนที่ 2.1: ตรวจสอบ Node.js

```bash
node --version
npm --version
```

**ต้องมี Node.js และ npm ติดตั้งแล้ว**

**ถ้ายังไม่มี:**
- ดาวน์โหลดจาก [nodejs.org](https://nodejs.org/)
- ติดตั้งตามปกติ

#### ขั้นตอนที่ 2.2: ติดตั้ง Dependencies

```bash
# เข้าไปในโฟลเดอร์ frontend
cd frontend

# ติดตั้ง dependencies
npm install
```

**รอให้ติดตั้งเสร็จ** (อาจใช้เวลา 1-2 นาที)

#### ขั้นตอนที่ 2.3: รัน Frontend

```bash
npm run dev
```

**ผลลัพธ์:**
- Frontend จะรันที่: **http://localhost:3000**
- เปิดเบราว์เซอร์ไปที่ URL นี้

**✅ ถ้าเห็นหน้าเว็บ YouTube Transcript Downloader แสดงว่าสำเร็จ!**

---

## 💻 วิธีใช้งาน

### วิธีที่ 1: ใช้ Frontend (แนะนำ)

1. **เปิด Frontend** ที่ http://localhost:3000

2. **กรอก YouTube URL หรือ Video ID**
   - ตัวอย่าง: `https://www.youtube.com/watch?v=dQw4w9WgXcQ`
   - หรือ: `dQw4w9WgXcQ`

3. **เลือกภาษา**
   - เลือกได้หลายภาษา (เช่น English, Thai)
   - ระบบจะลองภาษาแรกก่อน ถ้าไม่มีจะใช้ภาษาถัดไป

4. **เลือกรูปแบบไฟล์**
   - TXT - ไฟล์ข้อความธรรมดา
   - PDF - ไฟล์ PDF
   - DOCX - ไฟล์ Microsoft Word

5. **เลือก Options**
   - ✅ รวม Timestamps - แสดงเวลาในไฟล์
   - ✅ เก็บ HTML Formatting - เก็บรูปแบบ HTML (ถ้ามี)

6. **คลิกปุ่ม**
   - **📋 ดูรายการ Transcript** - ดู transcript ที่มีให้
   - **👁️ Preview** - ดูตัวอย่าง transcript ก่อนดาวน์โหลด
   - **⬇️ ดาวน์โหลด** - ดาวน์โหลดไฟล์

7. **ไฟล์จะถูกดาวน์โหลดอัตโนมัติ**

### วิธีที่ 2: ใช้ API โดยตรง

#### ตัวอย่างที่ 1: ดูรายการ Transcript

```bash
curl -X POST http://localhost:8000/api/transcripts/list \
  -H "Content-Type: application/json" \
  -d '{"url": "https://www.youtube.com/watch?v=dQw4w9WgXcQ"}'
```

#### ตัวอย่างที่ 2: ดาวน์โหลดเป็น PDF

```bash
curl -X POST http://localhost:8000/api/transcripts/download \
  -H "Content-Type: application/json" \
  -d '{
    "url": "https://www.youtube.com/watch?v=dQw4w9WgXcQ",
    "languages": ["en"],
    "file_format": "pdf",
    "include_timestamps": true
  }' \
  --output transcript.pdf
```

#### ตัวอย่างที่ 3: ใช้ Python

```python
import requests

# ดูรายการ transcript
response = requests.post(
    "http://localhost:8000/api/transcripts/list",
    json={"url": "https://www.youtube.com/watch?v=dQw4w9WgXcQ"}
)
print(response.json())

# ดาวน์โหลดเป็น PDF
response = requests.post(
    "http://localhost:8000/api/transcripts/download",
    json={
        "url": "https://www.youtube.com/watch?v=dQw4w9WgXcQ",
        "languages": ["en"],
        "file_format": "pdf"
    }
)

with open("transcript.pdf", "wb") as f:
    f.write(response.content)
```

---

## 🎨 ฟีเจอร์ Frontend

### 1. กรอก URL/Video ID
- รองรับ URL แบบเต็ม: `https://www.youtube.com/watch?v=VIDEO_ID`
- รองรับ Short URL: `https://youtu.be/VIDEO_ID`
- รองรับ Video ID โดยตรง: `VIDEO_ID`

### 2. เลือกภาษา
- เลือกได้หลายภาษา
- ภาษาแนะนำ: English, Thai, Chinese, Japanese, Korean, Spanish, French, German, Portuguese, Russian, Vietnamese, Indonesian

### 3. รูปแบบไฟล์
- **TXT** - ไฟล์ข้อความธรรมดา (.txt)
- **PDF** - ไฟล์ PDF (.pdf)
- **DOCX** - ไฟล์ Microsoft Word (.docx)

### 4. Options
- **รวม Timestamps** - แสดงเวลา [HH:MM:SS] ในไฟล์
- **เก็บ HTML Formatting** - เก็บรูปแบบ HTML เช่น `<i>`, `<b>`

### 5. ฟีเจอร์พิเศษ
- **ดูรายการ Transcript** - ดู transcript ที่มีให้สำหรับ video นี้
- **Preview** - ดูตัวอย่าง transcript ก่อนดาวน์โหลด (แสดง 50 snippets แรก)

---

## 🔧 การแก้ไขปัญหา (Troubleshooting)

### ปัญหา: Backend ไม่สามารถรันได้

**อาการ:** ได้ error `ModuleNotFoundError` หรือ `ImportError`

**วิธีแก้:**
```bash
# ตรวจสอบว่าเปิดใช้งาน virtual environment แล้ว
source venv/bin/activate  # Linux/Mac
venv\Scripts\activate     # Windows

# ติดตั้ง dependencies อีกครั้ง
pip install -r requirements.txt
```

### ปัญหา: Frontend ไม่สามารถเรียก API ได้

**อาการ:** ได้ error `CORS policy` หรือ `Network Error`

**วิธีแก้:**
1. ตรวจสอบว่า backend รันอยู่ที่ port 8000
2. ตรวจสอบว่า frontend รันอยู่ที่ port 3000
3. ตรวจสอบ CORS settings ใน `backend/main.py`

### ปัญหา: ไม่สามารถดาวน์โหลดไฟล์ได้

**อาการ:** ได้ error "ไม่พบ transcript" หรือไฟล์ไม่ถูกสร้าง

**วิธีแก้:**
1. ตรวจสอบว่า video มี subtitle หรือไม่
2. ลองใช้ปุ่ม "ดูรายการ Transcript" เพื่อดู transcript ที่มีให้
3. ลองเปลี่ยนภาษา
4. ตรวจสอบ logs ของ backend

### ปัญหา: Frontend ไม่สามารถรันได้

**อาการ:** ได้ error `command not found: npm`

**วิธีแก้:**
1. ติดตั้ง Node.js จาก [nodejs.org](https://nodejs.org/)
2. เปิด terminal ใหม่
3. ลองรัน `npm install` อีกครั้ง

---

## 📊 Stack ที่ใช้

### Backend Stack
- **FastAPI** - Web framework (เร็วและ modern)
- **Uvicorn** - ASGI server
- **youtube-transcript-api** - ดึง transcript จาก YouTube
- **reportlab** - สร้างไฟล์ PDF
- **python-docx** - สร้างไฟล์ DOCX
- **Pydantic** - Data validation

### Frontend Stack
- **React 18** - UI library
- **Vite** - Build tool (เร็วมาก)
- **Axios** - HTTP client
- **CSS3** - Styling (ไม่มี CSS framework)

---

## 🚀 การ Deploy สำหรับ Production

### Backend (Production)

```bash
# ติดตั้ง gunicorn
pip install gunicorn

# รันด้วย gunicorn
gunicorn main:app -w 4 -k uvicorn.workers.UvicornWorker --bind 0.0.0.0:8000
```

### Frontend (Production)

```bash
# Build
npm run build

# Serve static files (ใช้ nginx หรือ serve)
npm install -g serve
serve -s dist -l 3000
```

---

## 📝 ตัวอย่างการใช้งานจริง

### ตัวอย่างที่ 1: ดาวน์โหลด Transcript ภาษาไทยเป็น PDF

1. เปิด Frontend
2. กรอก URL: `https://www.youtube.com/watch?v=VIDEO_ID`
3. เลือกภาษา: Thai
4. เลือกรูปแบบ: PDF
5. ✅ รวม Timestamps
6. คลิก "ดาวน์โหลด"

### ตัวอย่างที่ 2: ดู Transcript ที่มีให้

1. กรอก URL
2. คลิก "ดูรายการ Transcript"
3. จะเห็นรายการ transcript ทั้งหมดที่มีให้
4. เลือกภาษาที่ต้องการ
5. ดาวน์โหลด

### ตัวอย่างที่ 3: Preview ก่อนดาวน์โหลด

1. กรอก URL
2. คลิก "Preview"
3. จะเห็นตัวอย่าง transcript (50 snippets แรก)
4. ถ้าพอใจแล้ว คลิก "ดาวน์โหลด"

---

## ✅ Checklist

- [ ] ติดตั้ง Python 3.8+ แล้ว
- [ ] ติดตั้ง Node.js แล้ว
- [ ] สร้าง virtual environment แล้ว
- [ ] ติดตั้ง backend dependencies แล้ว
- [ ] รัน backend server สำเร็จ
- [ ] ติดตั้ง frontend dependencies แล้ว
- [ ] รัน frontend สำเร็จ
- [ ] ทดสอบดึง transcript สำเร็จ
- [ ] ทดสอบดาวน์โหลดไฟล์สำเร็จ

---

## 🎓 แบบฝึกหัด

### แบบฝึกหัดที่ 1: ดาวน์โหลด Transcript เป็น TXT
1. เลือก YouTube video ที่มี subtitle
2. ดาวน์โหลดเป็นไฟล์ TXT
3. เปิดไฟล์เพื่อตรวจสอบ

### แบบฝึกหัดที่ 2: ดาวน์โหลด Transcript เป็น PDF
1. เลือก YouTube video
2. ดาวน์โหลดเป็นไฟล์ PDF
3. เปิดไฟล์ PDF เพื่อตรวจสอบ

### แบบฝึกหัดที่ 3: ใช้ API โดยตรง
1. ใช้ curl หรือ Python
2. เรียก API เพื่อดูรายการ transcript
3. ดาวน์โหลด transcript เป็นไฟล์

---

## 📚 เอกสารเพิ่มเติม

- [FastAPI Documentation](https://fastapi.tiangolo.com/)
- [React Documentation](https://react.dev/)
- [Vite Documentation](https://vitejs.dev/)
- [youtube-transcript-api GitHub](https://github.com/jdepoix/youtube-transcript-api)

---

**ขอให้สนุกกับการใช้งาน! 🎉**


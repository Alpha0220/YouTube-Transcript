# 📖 คู่มือการใช้งาน YouTube Transcript API - ขั้นตอนทีละขั้นตอน

## 🎯 วัตถุประสงค์
โปรเจกต์นี้ใช้สำหรับดึง transcript (คำบรรยาย) จาก YouTube videos โดยไม่ต้องใช้ browser หรือ API key

---

## 📋 ขั้นตอนการติดตั้งและใช้งาน

### ขั้นตอนที่ 1: ตรวจสอบ Python

เปิด Terminal (Command Prompt) และพิมพ์:

```bash
python3 --version
```

**ผลลัพธ์ที่ต้องการ:** Python 3.8 หรือสูงกว่า (แต่ต่ำกว่า 3.15)

**ถ้ายังไม่มี Python:**
- ดาวน์โหลดจาก [python.org](https://www.python.org/downloads/)
- ติดตั้งตามปกติ (อย่าลืมเลือก "Add Python to PATH")

---

### ขั้นตอนที่ 2: สร้าง Virtual Environment (แนะนำ)

**ทำไมต้องใช้ Virtual Environment?**
- แยก dependencies ของแต่ละโปรเจกต์
- ไม่ให้เกิด conflict กับโปรเจกต์อื่น

**คำสั่ง:**

```bash
# เข้าไปในโฟลเดอร์โปรเจกต์
cd /home/buggy/creatus/python/youtupe-transcrtip

# สร้าง virtual environment
python3 -m venv venv

# เปิดใช้งาน virtual environment
source venv/bin/activate  # Linux/Mac
# หรือ
venv\Scripts\activate     # Windows
```

**สังเกต:** เมื่อเปิดใช้งานแล้ว จะเห็น `(venv)` หน้าพรอมต์

---

### ขั้นตอนที่ 3: ติดตั้ง Library

```bash
pip install -r requirements.txt
```

หรือติดตั้งโดยตรง:

```bash
pip install youtube-transcript-api
```

**รอให้ติดตั้งเสร็จ** (อาจใช้เวลา 1-2 นาที)

---

### ขั้นตอนที่ 4: ทดสอบการใช้งาน

#### วิธีที่ 1: ใช้ไฟล์ตัวอย่างง่ายๆ

```bash
python example_simple.py
```

#### วิธีที่ 2: ใช้ไฟล์หลักพร้อม video ID

```bash
python main.py dQw4w9WgXcQ
```

หรือใช้ URL เต็ม:

```bash
python main.py "https://www.youtube.com/watch?v=dQw4w9WgXcQ"
```

---

## 💡 ตัวอย่างการใช้งานในโค้ด

### ตัวอย่างที่ 1: ดึง Transcript แบบพื้นฐาน

```python
from youtube_transcript_api import YouTubeTranscriptApi

# สร้าง instance
ytt_api = YouTubeTranscriptApi()

# ดึง transcript (ภาษาอังกฤษ)
transcript = ytt_api.fetch('VIDEO_ID')

# แสดงเนื้อหา
for snippet in transcript:
    print(f"[{snippet.start:.2f}s] {snippet.text}")
```

**อธิบาย:**
- `VIDEO_ID` = ID ของ video (เช่น จาก URL `https://www.youtube.com/watch?v=VIDEO_ID`)
- `snippet.start` = เวลาเริ่มต้น (วินาที)
- `snippet.text` = ข้อความ transcript

---

### ตัวอย่างที่ 2: ดึง Transcript ภาษาไทย

```python
from youtube_transcript_api import YouTubeTranscriptApi

ytt_api = YouTubeTranscriptApi()

# ลองภาษาไทยก่อน ถ้าไม่มีจะใช้ภาษาอังกฤษ
transcript = ytt_api.fetch('VIDEO_ID', languages=['th', 'en'])

print(f"ภาษา: {transcript.language}")
for snippet in transcript:
    print(snippet.text)
```

**อธิบาย:**
- `languages=['th', 'en']` = ลองภาษาไทยก่อน ถ้าไม่มีจะใช้ภาษาอังกฤษ
- สามารถใส่หลายภาษาได้ ระบบจะเลือกตามลำดับ

---

### ตัวอย่างที่ 3: ดูรายการ Transcript ที่มีให้

```python
from youtube_transcript_api import YouTubeTranscriptApi

ytt_api = YouTubeTranscriptApi()
transcript_list = ytt_api.list('VIDEO_ID')

print("รายการ transcript ที่มีให้:")
for transcript in transcript_list:
    transcript_type = "สร้างอัตโนมัติ" if transcript.is_generated else "สร้างด้วยมือ"
    print(f"• {transcript.language} ({transcript.language_code}) - {transcript_type}")
```

**อธิบาย:**
- `list()` = แสดงรายการ transcript ทั้งหมดที่มีให้
- `is_generated` = True ถ้าเป็น auto-generated subtitle

---

### ตัวอย่างที่ 4: บันทึกลงไฟล์

```python
from youtube_transcript_api import YouTubeTranscriptApi

ytt_api = YouTubeTranscriptApi()
transcript = ytt_api.fetch('VIDEO_ID', languages=['en'])

# บันทึกลงไฟล์
with open('transcript.txt', 'w', encoding='utf-8') as f:
    f.write(f"Video ID: {transcript.video_id}\n")
    f.write(f"ภาษา: {transcript.language}\n")
    f.write("=" * 50 + "\n\n")
    
    for snippet in transcript:
        f.write(f"[{snippet.start:.2f}s] {snippet.text}\n")

print("บันทึกเสร็จแล้ว!")
```

---

## 🔍 หา Video ID จาก YouTube URL

### วิธีที่ 1: จาก URL แบบปกติ
```
https://www.youtube.com/watch?v=dQw4w9WgXcQ
                                    ^^^^^^^^^^
                                    Video ID
```

### วิธีที่ 2: จาก Short URL
```
https://youtu.be/dQw4w9WgXcQ
                    ^^^^^^^^^^
                    Video ID
```

### วิธีที่ 3: ใช้ฟังก์ชันในโค้ด
```python
def get_video_id_from_url(url):
    if 'watch?v=' in url:
        return url.split('watch?v=')[1].split('&')[0]
    elif 'youtu.be/' in url:
        return url.split('youtu.be/')[1].split('?')[0]
    return url  # ถ้าใส่ ID โดยตรง
```

---

## ⚠️ ข้อควรระวัง

### 1. Video ไม่มี Subtitle
**ปัญหา:** ได้ error "No transcripts were found"

**วิธีแก้:**
- ตรวจสอบว่า video มี subtitle หรือไม่ (ดูที่ YouTube)
- บาง video อาจมีแต่ auto-generated subtitle
- ใช้ `list()` เพื่อดูว่ามี transcript อะไรบ้าง

### 2. Video มี Age Restriction
**ปัญหา:** ไม่สามารถดึง transcript ได้

**วิธีแก้:**
- ต้องใช้ cookie authentication
- ดูรายละเอียดใน [documentation](https://github.com/jdepoix/youtube-transcript-api)

### 3. Rate Limiting
**ปัญหา:** ได้ error 429 (Too Many Requests)

**วิธีแก้:**
- รอสักครู่แล้วลองใหม่
- ใช้ proxy (ดูใน documentation)

---

## 📁 โครงสร้างโปรเจกต์

```
youtupe-transcrtip/
├── main.py                 # ไฟล์หลักพร้อมฟีเจอร์ครบถ้วน
├── example_simple.py       # ตัวอย่างง่ายๆ สำหรับผู้เริ่มต้น
├── requirements.txt        # รายการ dependencies
├── README.md              # คู่มือภาษาอังกฤษ
├── คู่มือการใช้งาน.md      # คู่มือภาษาไทย (ไฟล์นี้)
└── .gitignore            # ไฟล์ที่ Git จะไม่ track
```

---

## 🎓 แบบฝึกหัด

### แบบฝึกหัดที่ 1: ดึง Transcript และแสดงผล
1. เลือก YouTube video ที่มี subtitle
2. ดึง transcript ภาษาอังกฤษ
3. แสดงผลในรูปแบบ: `[เวลา] ข้อความ`

### แบบฝึกหัดที่ 2: บันทึกลงไฟล์
1. ดึง transcript จาก video
2. บันทึกลงไฟล์ `.txt`
3. เปิดไฟล์เพื่อตรวจสอบ

### แบบฝึกหัดที่ 3: ดึงหลายภาษา
1. ใช้ `list()` เพื่อดู transcript ที่มีให้
2. ดึง transcript ภาษาไทย (ถ้ามี)
3. ถ้าไม่มี ให้ใช้ภาษาอังกฤษ

---

## 🆘 ปัญหาที่พบบ่อย (Troubleshooting)

### Q: ติดตั้ง library ไม่ได้
**A:** ตรวจสอบว่าเปิดใช้งาน virtual environment แล้วหรือยัง

### Q: ได้ error "ModuleNotFoundError"
**A:** ตรวจสอบว่าได้ติดตั้ง `youtube-transcript-api` แล้วหรือยัง

### Q: Video ไม่มี subtitle
**A:** ไม่ใช่ทุก video จะมี subtitle ลองหา video อื่นที่มี subtitle

### Q: ต้องการ subtitle ภาษาไทยแต่ไม่มี
**A:** ใช้ฟีเจอร์ translate (ดูใน README.md)

---

## 📚 เอกสารเพิ่มเติม

- [youtube-transcript-api PyPI](https://pypi.org/project/youtube-transcript-api/)
- [GitHub Repository](https://github.com/jdepoix/youtube-transcript-api)
- [Documentation](https://github.com/jdepoix/youtube-transcript-api#readme)

---

## ✅ Checklist การใช้งาน

- [ ] ติดตั้ง Python 3.8+ แล้ว
- [ ] สร้าง virtual environment แล้ว
- [ ] ติดตั้ง `youtube-transcript-api` แล้ว
- [ ] ทดสอบรัน `example_simple.py` สำเร็จ
- [ ] ทดสอบดึง transcript จาก video จริงแล้ว
- [ ] อ่านคู่มือนี้แล้ว

---

**ขอให้สนุกกับการใช้งาน! 🎉**
